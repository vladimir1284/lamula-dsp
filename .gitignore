# Source manuals (vendor copyright — reference only, never commit)
ravis_manual_md.zip
rvp900_manual_md.zip
ravis/
rvp900/

# mkdocs / wrangler build artifacts
site/
.wrangler/

# secrets
.env

# Rust
/target/
**/*.rs.bk

# tools/offline_build.sh: vendorizado local (fase 1) y su config de cargo
# (fase 2) — reproducibles bajo demanda, no se commitean (ver el propio
# script y docs/despliegue.md).
/vendor/
/.cargo/

# Python
__pycache__/
.pytest_cache/
.venv/
