[package]
name = "lamula-calibration"
description = "Conversión potencia lineal <-> dBZ del LAMULA DSP: constante de radar y corrección por r²."
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]

[dev-dependencies]
lamula-noise = { path = "../noise" }
lamula-simulator = { path = "../simulator" }
rand = "0.8"
